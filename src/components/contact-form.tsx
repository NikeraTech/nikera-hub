"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
export function ContactForm(){const[sent,setSent]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);const body=[`Name: ${d.get("name")}`,`Email: ${d.get("email")}`,`Phone: ${d.get("phone")}`,`Topic: ${d.get("topic")}`,"",String(d.get("message")),"",`Source: ${location.href}`,`Timestamp: ${new Date().toISOString()}`].join("\n");setSent(true);location.href=`mailto:hello@nikera.co.uk?subject=${encodeURIComponent(`Nikera Hub enquiry: ${d.get("topic")}`)}&body=${encodeURIComponent(body)}`}
return <form className="contact-form" onSubmit={submit}><div><label htmlFor="contact-name">Full name</label><input id="contact-name" name="name" required autoComplete="name"/></div><div><label htmlFor="contact-email">Email address</label><input id="contact-email" name="email" type="email" required autoComplete="email"/></div><div><label htmlFor="contact-phone">Phone number <small>Optional</small></label><input id="contact-phone" name="phone" type="tel" autoComplete="tel"/></div><div><label htmlFor="contact-topic">How can we help?</label><select id="contact-topic" name="topic"><option>Mortgage advice</option><option>Protection advice</option><option>Calculator question</option><option>Content feedback</option><option>Partnership enquiry</option><option>Something else</option></select></div><div className="form-wide"><label htmlFor="contact-message">Your message</label><textarea id="contact-message" name="message" rows={6} required/></div><label className="consent form-wide"><input type="checkbox" required/> <span>I agree that my details may be used to respond to this enquiry. See the <Link href="/privacy">privacy notice</Link>.</span></label><button className="form-wide" type="submit">Send enquiry →</button>{sent&&<p className="form-status form-wide" role="status">Your email application should now open with the enquiry prepared.</p>}</form>}
